package com.kbv.education.entity.enums;

/**
 * System roles for Phase 1. Additional roles (e.g. TEACHER, MENTOR) can be
 * appended in future phases without altering existing values.
 */
public enum RoleType {
    SUPER_ADMIN,
    STUDENT,
    PARENT
}
