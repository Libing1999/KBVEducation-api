package com.kbv.education.entity.enums;

/** Lifecycle state of an issued certificate. Only ISSUED exists today — a future
 * REVOKED state is a natural extension, not built out in Phase 5. */
public enum CertificateStatus {
    ISSUED
}
