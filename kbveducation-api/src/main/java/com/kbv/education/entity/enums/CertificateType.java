package com.kbv.education.entity.enums;

/** The four certificate kinds an admin can define a template for and issue. */
public enum CertificateType {
    TIER_1,
    TIER_2,
    TIER_3,
    COMPLETION
}
