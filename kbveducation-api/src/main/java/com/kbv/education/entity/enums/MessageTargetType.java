package com.kbv.education.entity.enums;

/**
 * Who a staff-composed {@code CoachMessage} is addressed to: a single named
 * student, or an entire cohort ("collective" — e.g. a shared win).
 */
public enum MessageTargetType {
    INDIVIDUAL,
    COLLECTIVE
}
